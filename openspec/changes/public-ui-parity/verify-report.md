```yaml
schema: gentle-ai.verify-result/v1
evidence_revision: sha256:87acb3032e6c7d889e51af110dd768858570d89dbfbd57ee28029a62a4f2bc5c
verdict: fail
blockers: 0
critical_findings: 4
requirements: 6/9
scenarios: 9/13
test_command: npm test
test_exit_code: 0
test_output_hash: sha256:cc4f8e6f706e2a0b78de15c0342ec06a35b8dfcb6dc99f15892c9c79794a1248
build_command: npm run build
build_exit_code: 0
build_output_hash: sha256:3d352a2be35941724d5930bf944857010ab8346d3845eb50a28c45b9d679821c
```

## Verification Report

**Change**: public-ui-parity
**Version**: N/A (delta specs; no version pin)
**Mode**: Standard (`strict_tdd: false` — confirmed in `openspec/config.yaml`)

### Completeness

| Metric | Value |
|--------|-------|
| Tasks total | 11 |
| Tasks complete | 11 |
| Tasks incomplete | 0 |

All 11 tasks are checked in `tasks.md`. Commits present locally: `5dfe63c` (slice 1), `09a1c25` (slice 2), `a0006f8` (slice 3), planning `9fb096a`. Working tree is clean except the untracked `apply-progress.md` (not a slice deliverable). Slice 2 carries a maintainer-approved `size:exception` (575 authored lines) — recorded as context, not a finding.

### Build & Tests Execution

Every gate below was re-run by this verifier on a fresh production build with zero stray `next-server`/`next start` processes (confirmed via `ps -eo pid,etimes,args | grep -E 'next[-]server|next st[a]rt'` → none). `next-env.d.ts` was regenerated by `build` and reverted with `git checkout -- next-env.d.ts`.

**Build**: ✅ Passed
```text
npm run build
▲ Next.js 16.2.9 (Turbopack)
✓ Compiled successfully in 1444ms
Route (app)
┌ ƒ /
├ ○ /_not-found
└ ƒ /reservar
```

**Tests (Vitest)**: ✅ 80 passed / 0 failed / 0 skipped
```text
npm test → 10 files, 80 tests passed (v2.1.9)
```

**Typecheck**: ✅ exit 0
```text
npm run typecheck → tsc --noEmit, no diagnostics
```

**Lint**: ✅ exit 0
```text
npm run lint → eslint ., no warnings
```

**E2E (Playwright)**: ✅ 15 passed (Chromium, production server)
```text
npm run test:e2e → 15 passed (4.4s)
```

**Coverage**: ➖ Not available (no coverage instrumentation; `strict_tdd: false`)

### Spec Compliance Matrix

| Requirement | Scenario | Test | Result |
|-------------|----------|------|--------|
| Deterministic date scroller | Full window with closed leading day | `AvailabilityCalendar.test.tsx` > `renders one date card per returned day…`, `disables exactly the days…`, `selects the first open day…` | ✅ COMPLIANT |
| Deterministic date scroller | Select an open day | `AvailabilityCalendar.test.tsx` > `activates another open card and replaces both the selection and the rendered slots` | ✅ COMPLIANT |
| Client-derived time groups | Boundary slots | `AvailabilityCalendar.test.tsx` > `places the 11:59/12:00/17:59/18:00 boundary slots…`, `renders the selected day slots under Mañana, Tarde and Noche…` | ✅ COMPLIANT |
| Booking step structure and controls | Render step chrome | `public-availability.spec.ts` > `renders the read-only step chrome, chip, professional pill and jump control` | ✅ COMPLIANT |
| Booking step structure and controls | Read-only completion | `public-availability.spec.ts` > `reaches the read-only end of flow without creating, updating, or reserving anything`; `AvailabilityCalendar.test.tsx` > `reaching the final state performs no fetch…` | ✅ COMPLIANT |
| Database-authoritative public copy | Database values differ from prototype | `profile.spec.ts` > `renders hero and service catalog from the public DTOs`, `renders the database shop name in a two-tone logo…`; `PublicInfo.test.tsx` > `prefixes the literal Hoy…`; crumb asserted in `public-availability.spec.ts` chrome test | ✅ COMPLIANT |
| No rating data or presentation | Prototype contains an unsupported rating | `ServiceCatalog.test.tsx` > `renders no rating row…`; `profile.spec.ts` > `…no rating row`; `public-availability.spec.ts` chrome test (negative `★`/`reseñas`) | ✅ COMPLIANT |
| Prototype booking visual fidelity | Inspect booking controls | no runtime assertion of slot `14.5px/600`/r12, group-label `12px/600`, pill 26px/999px (source-only in `app/globals.css`) | ❌ UNTESTED |
| Theme switch | Toggle swaps theme | `ThemeSwitch.test.tsx` + `profile.spec.ts` prove `data-theme` swap only; heading-font Playfair↔Oswald swap is not asserted | ❌ UNTESTED |
| Theme switch | Strong-fill token exists in both themes | no assertion that `--fill-strong` is defined or that `.prof-avatar` resolves from it (source-only) | ❌ UNTESTED |
| Theme-specific fidelity tokens and brand mark | Shadow and typography isolation | no assertion of theme-scoped shadow rgba / letter-spacing / per-element tracking (source-only) | ❌ UNTESTED |
| Theme-specific fidelity tokens and brand mark | Database logo split | `PublicHeader.test.tsx` > `splitLogoName…`, `renders the database shop name with only the last word in the brass span`; `profile.spec.ts` > `renders the database shop name in a two-tone logo` (asserts computed brass color) | ✅ COMPLIANT |
| Accessible service disclosure | Expand and collapse service detail | `ServiceCatalog.test.tsx` > `toggles the service detail with a button that owns aria-expanded and aria-controls` (asserts `details` absent, `aria-expanded` false→true→false, region visible/hidden) | ✅ COMPLIANT |

**Compliance summary**: 9/13 scenarios runtime-compliant; 4 scenarios UNTESTED (no covering test).

### Correctness (Static Evidence)

| Requirement | Status | Notes |
|------------|--------|-------|
| Deterministic date scroller | ✅ Implemented | 14 cards from `days.map()`, labels from DTO `day` + `date` string slices, `disabled = slots.length === 0`, first-open-day init via `findIndex`, no `Date`/`Intl`/effect |
| Client-derived time groups | ✅ Implemented | `bucketSlots` parses hour before `T`, `<12`/`<18`, empty headings omitted |
| Booking step structure and controls | ✅ Implemented | modal topbar `/` links, DB-name crumb, service chip, static pill, scroll/focus jump (no dialog), read-only final state preserved |
| Database-authoritative public copy | ✅ Implemented | DB name/description/hours; literal `Hoy ` prefix; no prototype literals in rendered surface |
| No rating data or presentation | ✅ Implemented | no `★`/`reseñas`/`.rating-row` anywhere in source; no rating column/DTO field; 3 negative guards |
| Prototype booking visual fidelity | ✅ Source-defined | exact values in `app/globals.css`; NOT runtime-asserted (CRITICAL-1) |
| Theme switch | ✅ Implemented | `--fill-strong` first-class in both themes; heading font swap in `:root`/dark; NOT runtime-asserted (CRITICAL-2, CRITICAL-3) |
| Theme-specific fidelity tokens and brand mark | ✅ Source-defined | theme-scoped shadows/letter-spacing; logo split runtime-proven; shadow/typography isolation NOT runtime-asserted (CRITICAL-4) |
| Accessible service disclosure | ✅ Implemented | native button, `aria-expanded`/`aria-controls`, hidden region, 180° chevron |

### Coherence (Design)

| Decision | Followed? | Notes |
|----------|-----------|-------|
| Crumb data via `loadPublicPageData(slug)` | ✅ Yes | `app/reservar/page.tsx` composes `loadAvailability` + `loadPublicPageData` in `Promise.all`; crumb uses `context.barberia.name` |
| Strong fill light `#1A1A1A` / dark `#0E0E0F` | ✅ Yes | `--fill-strong` set in both `:root` blocks; `--brand-mark-bg` kept as alias |
| Island-local `bucketSlots`, no `Date` | ✅ Yes | exported from `AvailabilityCalendar.tsx`, string parsing only |
| Logo `splitLogoName` pure helper | ✅ Yes | exported from `PublicHeader.tsx` |
| `lib/public-format.ts` shared formatters | ✅ Yes | chip and catalog both import `formatDuration`/`formatPrice` |
| Selection `{ dayIndex, slot }`, `-1` all-empty, no post-hydration flip | ✅ Yes | `useState` initializer computes `findIndex`; no effect |
| Final/jump honest copy, scroll/focus only | ✅ Yes | `Elegir otro horario` preserved verbatim; jump only scrolls/focuses |

### E2 Exclusion

Confirmed: the rating row is absent from both rendered surfaces (landing `/` and `/reservar`) and no rating column or DTO field exists.

- No `★`, `reseñas`, `.rating-row`, or `rating` identifier anywhere in source, edge functions, or migrations except the three negative tests themselves (`grep` over `supabase/**`, `types/**`, `components/**`, `app/**`).
- `types/booking.ts` and `types/public.ts` carry no rating/score/review field and are unchanged by this change.
- `supabase/migrations/` contains no rating column (no migration in the change diff; `phase10`/`phase11`/`phase12` are untouched).
- Three negative guards: `ServiceCatalog.test.tsx` (unit), `profile.spec.ts` (landing e2e), `public-availability.spec.ts` (booking e2e) all assert `★`/`reseñas`/`.rating-row` absence.

### Contract Non-Drift

`git diff --name-only 6e1ea89..HEAD` shows no change to `types/booking.ts`, `lib/availability.server.ts`, `supabase/functions/public-availability/index.ts`, or any `supabase/migrations/*`. The only new files are `lib/public-format.ts` (pure formatters), `components/public/ServiceDisclosure.tsx` (client component), and `components/public/PublicHeader.test.tsx` (test). No new fetch endpoint, DTO, RPC, Edge Function, or migration was introduced.

### Hydration Risk

Confirmed from source: the booking island (`AvailabilityCalendar.tsx`, the only client component in the booking flow) constructs no `Date`, uses no `Intl`/`toLocale`/`toLocaleString`, and runs no `useEffect`. Labels derive from the DTO `day` index and `date` string slices; the initial selection is computed deterministically in the `useState` initializer (`days.findIndex(day => day.slots.length > 0)`). `formatPrice` uses `toLocaleString('es-AR')` but only in server components (`ServiceCatalog.tsx`, `app/reservar/page.tsx`), never in a client render path. `ServiceDisclosure` uses `useId`/`useState` only. Hydration is safe.

### Issues Found

**CRITICAL** (4 — each is a spec scenario with no runtime-covering test; all are the class the design declared "review evidence, not tests"):

1. **CRITICAL-1 — REQ-6 "Inspect booking controls" is UNTESTED.** The spec requires the slot (`14.5px`, `600`, mono, radius-12), group-label (muted, uppercase, `12px`, `600`), and professional pill (26px avatar, chevron) to match the prototype treatment "when computed styles … are inspected". The e2e `renders the read-only step chrome…` asserts the pill structure (avatar + chevron present, no `.prof-select-label`) and accessible text, but no test reads the computed `font-size`/`font-weight`/`border-radius`/`width`. The values exist verbatim in `app/globals.css` (`.availability-slot` at 709-719, `.group-label` at 694-701, `.prof-select`/`.prof-avatar` at 545-567) — this is source evidence, not runtime proof.
2. **CRITICAL-2 — REQ-7 "Toggle swaps theme" is UNTESTED for its THEN clause.** `ThemeSwitch.test.tsx` and `profile.spec.ts` (`applies the stored theme before paint and toggles it`) prove the `data-theme` attribute flips and persists, but the scenario explicitly requires the heading font to swap Playfair Display ↔ Oswald. No test asserts the rendered heading `font-family`. The swap exists only as `--heading-font` in `:root` vs `:root[data-theme='dark']`.
3. **CRITICAL-3 — REQ-7 "Strong-fill token exists in both themes" is UNTESTED.** `--fill-strong` is defined in both `:root` blocks (`#1a1a1a` light, `#0e0e0f` dark) and `.prof-avatar` uses `background: var(--fill-strong)`, but no test asserts the token is defined for the active theme or that the avatar background resolves from it. Source evidence only.
4. **CRITICAL-4 — REQ-8 "Shadow and typography isolation" is UNTESTED.** Theme-scoped shadows (`--brand-mark-shadow`, `--ticket-shadow`) and per-element letter-spacing (`.logo` 0.04em, `.section-title` 0.06em/0.03em, hero/ticket none) are defined in `app/globals.css`, but no test asserts the computed shadow or letter-spacing, nor that no value leaks across themes. Source evidence only.

**What must change (per CRITICAL) — reported, not fixed here:**
- The codebase already proves computed-style assertions are feasible: `profile.spec.ts:94-103` reads `getComputedStyle(...).color` against the live `--brass` token. The same pattern can assert (a) slot/group-label/pill computed styles, (b) heading `font-family` flip after toggle, (c) `--fill-strong` definition + `.prof-avatar` resolved background, and (d) theme-scoped shadow/letter-spacing. Alternatively, amend the two delta specs to scope the executable-verifiable subset (structure, accessible text, logo split, disclosure — already tested) and move the pure-pixel fidelity/typography/token-value assertions to a manual acceptance criterion. Until one of those happens, these scenarios remain UNTESTED and the change cannot pass verification under the "compliant only when a covering test passed at runtime" rule.

**WARNING**: None beyond the four CRITICALs above.

**SUGGESTION** (all seven apply-progress deferred findings independently confirmed; plus one new):
1. Dead CSS: `app/globals.css:725-729` groups `.availability-empty, .availability-day-empty`, but no component renders `availability-day-empty`.
2. `onClick` is attached to every date card including disabled ones (`AvailabilityCalendar.tsx:213`); inert via native `disabled` + `pointer-events:none` and covered by a unit test, but it deviates from the "no activation handler" design pin.
3. `aria-pressed` is used as the single-select indicator (`:211`); a `radiogroup`/`radio` or `aria-current` is more faithful for a single-select group. Tied to the failed accessibility research lane.
4. Slice 3 dropped the `.ticket-details p { margin: 0 }` reset; the expanded detail `<p>` regains browser default margins (`.ticket-detail` has no `p` reset).
5. Brass tail binds through the pre-existing global `.logo span` selector rather than a dedicated class — implicit coupling.
6. Hard-coded `Conexión Barbería` literals remain in metadata fallbacks (`app/layout.tsx:16`, `app/reservar/page.tsx:19`, `app/page.tsx:28`); predate this change and are not rendered surface, but are not database-authoritative.
7. `app/not-found.tsx` renders an empty logo slot (no DB name on an error page); deliberate and tested, but the brand mark is visually blank on 404.
8. NEW: `ThemeSwitch.tsx:11` comment "The only Client Component on the public landing" is now stale — slice 3 added `ServiceDisclosure` as a second client component on `/`.

### Verdict

FAIL

The behavioral and structural requirements are implemented correctly and every executable gate passes (80 unit + 15 e2e + typecheck + lint + build; contract non-drift and the E2 rating exclusion confirmed). However, 4 of 13 spec scenarios — the visual-fidelity and token-value scenarios (booking control computed styles, heading-font swap, `--fill-strong` avatar binding, theme shadow/letter-spacing isolation) — have no runtime-covering test. They are source-defined but UNTESTED, and the hard rule "a spec scenario is compliant only when a covering test passed at runtime" makes each one CRITICAL. The fix is either to add the computed-style assertions the codebase already demonstrates for `--brass`, or to amend the specs to scope the untestable fidelity values to manual acceptance. Neither is done here by design (this verifier is read-only with respect to source and specs).
