# Testing Capabilities — SDD Initialization (conexion-demo)

**Discovered Projects:**
- conexion-demo

**Stack:**
- Astro + React (islands)
- TypeScript
- Supabase Edge Functions
- PostgreSQL + RLS

**Automated Test Runner:** Not detected (no package.json, no CI, no test infra)

**Strict TDD:** false

**Explanation:**
This is a greenfield project, no test, lint, or formatter infra exists yet. Strict TDD cannot be enforced until baseline test runner is established and covers the workspace. You may add initial test scaffolding in /openspec or PR with an initial test/lint commit before raising new changes.
