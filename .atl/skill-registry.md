# .atl/skill-registry.md — SDD Skill Registry for 'conexion-demo'

## Registered skills
- sdd-init: Initialize SDD context, detect stack, persistence, testing, registry
- skill-registry: Build contextual skill index (update after new skill install or removal)

(skill list is static; update registry as skills are added)

### File structure
- openspec/config.yaml — workspace/init config
- openspec/testing-capabilities.md — test/CI runner, TDD, project scan facts
- .atl/skill-registry.md — skill registry for orchestrator/agents
